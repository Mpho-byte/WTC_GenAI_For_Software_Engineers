# Getting Started with FastAPI

# Understanding FastAPI Fundamentals

**Prompt Used**

What is FastAPI and how does it compare to Flask and Django? 

Explain the core concepts and terminology used in FastAPI.

What are the key advantages of FastAPI over other Python web frameworks?

Create a glossary of essential FastAPI terms and concepts.

**AI Summary**
FastAPI is a modern Python web framework used to build APIs quickly and efficiently.

Comparison:
- Flask: lightweight but requires more manual setup
- Django: full-stack framework with more built-in features
- FastAPI: focused on APIs, fast, and supports async programming

Key Concepts:
- Path operations (routes/endpoints)
-  Pydantic models (data validation)
-  Dependency injection
-  Async support

Advantages:
- Very fast performance- Automatic API documentation (Swagger UI)
- Built-in data validation
- Easy to learn and use### 

Glossary:
- Endpoint: URL route in the API
-  Pydantic: data validation library
- Uvicorn: ASGI server used to run FastAPI
- Request body: data sent to API

# Creating Your First API

**Prompt Used**

Generate a basic "Hello World" FastAPI example with explanations.Explain how to run and test it locally.

**Code Implementation**

(Hello World API)
from fastapi import FastAPIapp = FastAPI()@app.get("/")async def root():    return {"message": "Hello World from FastAPI!"}@app.get("/items/{item_id}")async def get_item(item_id: int):    return {"item_id": item_id}

**Run Instructions**
pip install fastapi uvicornuvicorn main:app --reload
Visit:

http://127.0.0.1:8000/

http://127.0.0.1:8000/docs

# Enhancing Your API

**Prompt Used**

```md id="enh1p"How do I add request body validation with Pydantic models in FastAPI?Show error handling and project structure.

**AI Summary**

FastAPI uses Pydantic models to validate incoming request data.

Key ideas:
-Models define expected data structure
- FastAPI automatically validates input
- Errors are returned automatically if invalid

Example:- 
Use BaseModel for request validation- Use routers to organize code- Use exception handlers for errors

Benefits:
- Cleaner code
- Safer APIs
- Better maintainability

# Exercise Challenge (To-Do API)

**Prompt Used**

Create a FastAPI to-do API with:- Create task- List tasks- Mark complete- Delete task

**Implementation (Simple Version)**

from fastapi import FastAPIapp = FastAPI()todos = []@app.post("/todos")def create_todo(todo: dict):    todos.append(todo)    return todo@app.get("/todos")def get_todos():    return todos@app.put("/todos/{index}")def mark_complete(index: int):    todos[index]["completed"] = True    return todos[index]@app.delete("/todos/{index}")def delete_todo(index: int):    
return todos.pop(index)

**Reflection**

FastAPI was easier to understand than expected because of its simple structure and automatic documentation.
I learned that:
- FastAPI is built for APIs, not full websites
- Pydantic helps with data validation automatically
- Async functions improve performance
- The built-in docs make testing very easyThe most important improvement I noticed is how FastAPI reduces boilerplate code compared to Flask or Django.Overall, this exercise helped me understand how modern APIs are built in Python and how AI can guide me step by step in learning new frameworks.
